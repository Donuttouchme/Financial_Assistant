[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    real_fx_client: tests that exercise the real frankfurter.app HTTP client with their own httpx mock; opts out of the autouse fixture that patches fetch_rates_*.
